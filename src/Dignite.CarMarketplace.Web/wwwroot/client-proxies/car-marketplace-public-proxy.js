/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module car-marketplace-public

(function(){

  // controller dignite.carMarketplace.public.dealers.dealer

  (function(){

    abp.utils.createNamespace(window, 'dignite.carMarketplace.public.dealers.dealer');

    dignite.carMarketplace.public.dealers.dealer.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/dealer/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dignite.carMarketplace.public.dealers.dealer.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/dealer' + abp.utils.buildQueryString([{ name: 'filter', value: input.filter }, { name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dignite.carMarketplace.public.cars.brand

  (function(){

    abp.utils.createNamespace(window, 'dignite.carMarketplace.public.cars.brand');

    dignite.carMarketplace.public.cars.brand.getList = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/brand',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dignite.carMarketplace.public.cars.configurationItem

  (function(){

    abp.utils.createNamespace(window, 'dignite.carMarketplace.public.cars.configurationItem');

    dignite.carMarketplace.public.cars.configurationItem.getList = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/configuration-item',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dignite.carMarketplace.public.cars.model

  (function(){

    abp.utils.createNamespace(window, 'dignite.carMarketplace.public.cars.model');

    dignite.carMarketplace.public.cars.model.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/model' + abp.utils.buildQueryString([{ name: 'brandId', value: input.brandId }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dignite.carMarketplace.public.cars.saleCar

  (function(){

    abp.utils.createNamespace(window, 'dignite.carMarketplace.public.cars.saleCar');

    dignite.carMarketplace.public.cars.saleCar.create = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/sale-car',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

  // controller dignite.carMarketplace.public.cars.trim

  (function(){

    abp.utils.createNamespace(window, 'dignite.carMarketplace.public.cars.trim');

    dignite.carMarketplace.public.cars.trim.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/trim/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dignite.carMarketplace.public.cars.trim.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/trim' + abp.utils.buildQueryString([{ name: 'modelId', value: input.modelId }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller dignite.carMarketplace.public.cars.usedCar

  (function(){

    abp.utils.createNamespace(window, 'dignite.carMarketplace.public.cars.usedCar');

    dignite.carMarketplace.public.cars.usedCar.get = function(id, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/usedcar/' + id + '',
        type: 'GET'
      }, ajaxParams));
    };

    dignite.carMarketplace.public.cars.usedCar.getList = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/car-marketplace-public/usedcar' + abp.utils.buildQueryString([{ name: 'filter', value: input.filter }, { name: 'brandId', value: input.brandId }, { name: 'modelId', value: input.modelId }, { name: 'dealerId', value: input.dealerId }, { name: 'color', value: input.color }, { name: 'minRegistrationDate', value: input.minRegistrationDate }, { name: 'maxRegistrationDate', value: input.maxRegistrationDate }, { name: 'minTotalMileage', value: input.minTotalMileage }, { name: 'maxTotalMileage', value: input.maxTotalMileage }, { name: 'minPrice', value: input.minPrice }, { name: 'maxPrice', value: input.maxPrice }, { name: 'transmissionType', value: input.transmissionType }, { name: 'powerType', value: input.powerType }, { name: 'modelLevel', value: input.modelLevel }, { name: 'sorting', value: input.sorting }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

  })();

})();


