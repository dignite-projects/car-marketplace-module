import { GetDealerStatusNamePipe } from './get-dealer-status-name.pipe';

describe('GetDealerStatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDealerStatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
