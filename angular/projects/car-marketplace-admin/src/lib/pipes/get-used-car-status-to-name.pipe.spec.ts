import { GetUsedCarStatusToNamePipe } from './get-used-car-status-to-name.pipe';

describe('GetUsedCarStatusToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetUsedCarStatusToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
