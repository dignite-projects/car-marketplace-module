import { GetDealerStatusToNamePipe } from './get-dealer-status-to-name.pipe';

describe('GetDealerStatusToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDealerStatusToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
